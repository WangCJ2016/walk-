export interface Auth {
    userName?: string,
    token?: string,
    sign_type?: string,
    name?: string
    image?: string
    account?: string
    sex?: string
    address?: string,
    sign?: string,
    phoneNum?: string,
    code?: string,
    msg?: string
}